# Example .env file for doc2lora R2 integration
# Copy this file to .env and fill in your actual credentials

# R2 API Credentials
AWS_ACCESS_KEY_ID=your-r2-access-key-id
AWS_SECRET_ACCESS_KEY=your-r2-secret-access-key

# R2 Endpoint URL (replace with your account ID)
R2_ENDPOINT_URL=https://your-account-id.r2.cloudflarestorage.com

# R2 Bucket Configuration (optional - can be specified in code)
R2_BUCKET_NAME=my-documents-bucket
R2_FOLDER_PREFIX=training-docs

# To use this file:
# 1. Copy to .env: cp .env.example .env
# 2. Edit .env with your actual credentials
# 3. Use with CLI: doc2lora convert-r2 my-bucket --env-file .env
# 4. Use with Python: convert_from_r2(bucket_name="my-bucket", env_file=".env")
