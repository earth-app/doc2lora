# full install (equivalent to: pip install "doc2lora[all]")
# core runtime
torch>=1.13.0
transformers>=4.21.0
peft>=0.3.0
datasets>=2.0.0
click>=8.0.0
tqdm>=4.64.0
numpy>=1.21.0
pandas>=1.3.0
# document/format parsers ([docs])
pypdf>=3.0.0
python-docx>=0.8.11
beautifulsoup4>=4.11.0
lxml>=4.9.0
pyyaml>=6.0
openpyxl>=3.0.0
python-pptx>=0.6.21
odfpy>=1.4.1
striprtf>=0.0.26
EbookLib>=0.18
py7zr>=0.20.0
# image text recognition / OCR ([image]); needs the system tesseract-ocr binary
pytesseract>=0.3.10
Pillow>=9.0.0
# audio transcription ([audio]); faster-whisper is the default backend.
# faster-whisper/openai-whisper need the ffmpeg binary; the SpeechRecognition
# fallback needs ffmpeg only for non-wav (via pydub)
faster-whisper>=1.0.0
openai-whisper>=20231117
SpeechRecognition>=3.10.0
pydub>=0.25.1
# video ingestion ([video]): per-frame OCR (opencv) + audio transcript (whisper)
opencv-python>=4.7.0
# cloudflare r2 ingestion ([r2])
boto3>=1.26.0
python-dotenv>=1.0.0
# 4-bit QLoRA, cuda only ([quant])
bitsandbytes>=0.41.0
